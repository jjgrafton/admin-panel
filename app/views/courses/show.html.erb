<h2 class="admin-form"> <%= @course.name %></h2>

<p class="body-text">Course: <%= @cohort.course %></p>
<p class="body-text">Start Date: <%= @cohort.start_date %></p>
<p class="body-text">End Date: <%= @cohort.end_date %></p>
<p class="body-text">Instructor: <%= @cohort.instructor %></p>
<p class="body-text">Students: <%= @cohort.students %></p>


<hr>
<%= link_to "Edit", edit_instructor_path(@instructor), class: 'btn btn-default', class: 'admin-form' %>
<%= link_to "Delete", instructor_path(@instructor), method: :delete, data: {confirm: 'Are you sure?'}, class: 'btn btn-danger' %>