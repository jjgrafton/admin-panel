<h1>Instructors</h1>

<% @instructors.each do |instructor| %>
<div class="well">
    <h2><%= instructor.first_name %></h2>
        <h2><%= instructor.last_name %></h2>
    <%= link_to "Full Profile", instructor_path(instructor), :class => 'btn btn-primary' %>
</div>


<% end %>