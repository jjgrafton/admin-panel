
<%= render 'shared/dashboard' %>

<h2 class="admin-form-header"> <%= @student.first_name %> <%= @student.last_name %></h2><br>


<p class="admin-form">Date of Birth: <%= @student.d_o_b %></p>
<p class="admin-form">Highest Completed Education: <%= @student.highest_ed %></p>
<p class="admin-form">ID Number: <%= @student.id %></p>
<p class="admin-form">Course: <%= @student.course_id %></p>
<div class="admin-display"><p class="admin-form">Cohort: <%= @student.cohort_id %></p> <%= link_to "More Info", cohort_path(@cohort), class: 'btn btn-default', class: 'admin-form-orange' %></div>






<hr>
<%= link_to "Edit", edit_student_path(@student), class: 'btn btn-default', class: 'admin-form' %>
<%= link_to "Delete", student_path(@student), method: :delete, remote: true,   class: 'admin-form-orange', 'data-confirm': 'Are you sure?' %>

