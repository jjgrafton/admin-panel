<h1 class="admin-form">New Student</h1>
<%= form_for :student, url: students_path do |f| %>
    <% if @student.errors.any? %>
        <% @student.errors.full_messages.each do |msg| %>
            <div class="alert alert-danger"><%= msg %></div>
        <% end %>
    <% end %>
    <p>
        <%= f.label(:first_name, class: 'admin-form') %><br>
        <%= f.text_field(:first_name, class: 'form-control', class: 'admin-form') %>
    </p>
    <p>
        <%= f.label(:last_name, class: 'admin-form') %><br>
        <%= f.text_field(:last_name, class: 'form-control', class: 'admin-form') %>
    </p>

    <p>
        <%= f.label(:d_o_b, class: 'admin-form') %><br>
        <%= f.text_field(:d_o_b, class: 'form-control', class: 'admin-form') %>
    </p>

    <p>
        <%= f.label(:highest_ed, class: 'admin-form') %><br>
        <%= f.text_field(:highest_ed, class: 'form-control', class: 'admin-form') %>
    </p>

    <p>
        <%= f.submit(class: 'btn btn-primary', class: 'admin-form') %>
    </p>

    <% end %>